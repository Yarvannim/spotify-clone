export interface Song {
    id: string;
    title: string;
    artist: string;
}

export interface SongSearchResponse {
    id: string;
    title: string;
    artist: string;
}

export interface StreamUrlResponse {
    streamUrl: string;
}